# 双语电子书生成流水线 · 设计文档 v2（含现状审计与修订）

> **修订日期**：2026-09-12
> **修订缘由**：对已产出的第一本成品（《智人之上》Nexus 双语版）做了实测审计，发现文档描述的方案与真实实现存在多处漂移，且暴露了三个结构性问题。本文档在 v1 全文基础上追加**实测数据、诊断结论、修订提案与修订后路线图**。
> **阅读顺序**：接手者请**跳过 Part I 直接看 Part II**，先理解现状为什么烂，再回头读原设计。Part I 为 v1 原文保留，仅做编号整理，内容未改。

---

# Part I · v1 原始设计（保留）

## 1. 项目定位与核心原则

- **本质**：语义对齐 + 格式保真。英文 epub 是唯一骨架（AST/结构树），中文译文只是"血肉"，填进去。
- **核心原则**：
  1. **格式忠实于英文**：以英文 epub 的结构为权威，中文不反向改造结构。
  2. **学术/技术元素不可碰**：数学环境、公式、代码片段、代码块、URL 等保持原样。
  3. **小节级对齐**：LLM 以小节（section）为单位做段落对齐，颗粒度小 → 上下文闭合 → 幻觉极低。
  4. **全自动**：无人工介入、无人工清洗环节。本项目是个人兴趣驱动的自动化流水线。
- **完成度定义**：跑完第一阶段（对齐+回填）即为可用产品（适用于排版简单的书）；第二阶段（校对）是质量增强。

## 2. 输入与输出

| 项 | 内容 |
|---|---|
| 英文输入 | 干净的 epub（权威骨架） |
| 中文输入 | Markdown / PDF / epub（质量良好，格式错误不多） |
| PDF 预处理 | 扫描版与文字版 PDF 一律先经 **MinerU** 高精度解析转为 md |
| 输出 | 双语 epub：每段英文紧跟一段中文，样式忠实英文骨架 |
| 阅读器目标 | 弱渲染（微信读书）与强渲染（readest）双档适配，见 §7 |

## 3. 总体流水线

```
[英文 epub]──解析──► 章→小节→段落（带类型标注）──┐
                                              ├─► 小节级 LLM 对齐（mapping）
[中文 md/epub/pdf]──(pdf先经MinerU)──解析──► 段落 ─┘        │
                                                        ▼
                                              回填：中文插入英文 epub（双语 epub 生成）★第一阶段完成
                                                        │
                                                        ▼
                                              分批校对（~20 对/批）→ 封闭枚举勘误
                                                        │
                              ┌─────────────────────────┼──────────────────┐
                              ▼                         ▼                  ▼
                        漏译 → 重译流程          格式错误 → 程序修复     棘手 → LLM 修复
                              └─────────────────────────┴──────────────────┘
                                                        ▼
                                            重新回填 → 目标阅读器渲染后端（公式编译）
```

## 4. 中间表示（IR）规范

整条流水线在第一阶段解析后收敛到统一 IR，之后对齐/校对/渲染只认 IR。解析器（epub/md/pdf）与渲染器（epub/md/html）各自独立、可替换。

### 4.1 索引规则（核心）

1. **小节内数组下标 = 段落 id**，不设全局字符串 id。
2. **pair = `{"en": int[], "zh": int[]}`**，引用两侧段落数组下标。关系类型由数组长度自然表达：

| 关系 | en | zh | 场景 |
|---|---|---|---|
| 1:1 | `[3]` | `[3]` | 绝大多数 |
| 1:N | `[3]` | `[3,4,5]` | 英文一段，中文拆三段 |
| N:1 | `[3,4]` | `[3]` | 英文两段，中文合并 |
| 1:0 | `[3]` | `[]` | 漏译 → 走重译流程 |
| 0:1 | `[]` | `[3]` | 多余中文 → 走勘误删除 |

3. **覆盖性硬约束**：每个 en 段与每个 zh 段恰好出现在一个 pair 中。

### 4.2 段落结构：稀疏存储 + 单字母键

大多数段落是普通段落，只有 `t` 字段；特殊段落才带 `y`（type）/`s`（style）。

### 4.3 IR 顶层结构

```json
{
  "meta": {
    "title_en": "How to Solve It",
    "title_zh": "怎样解题",
    "source": { "en": "en.epub", "zh": "zh.md" },
    "target_reader": "readest"
  },
  "chapters": [
    {
      "idx": 0,
      "title_en": "Chapter 1",
      "title_zh": "第一章",
      "sections": [
        {
          "idx": 0,
          "title_en": "1.1 Understanding the Problem",
          "title_zh": "1.1 理解题目",
          "en": [
            { "t": "Text paragraph ...", "s": "para" },
            { "t": "$E=mc^2$", "y": "math-block" },
            { "t": "def f(): ...", "y": "code-block" }
          ],
          "zh": [
            { "t": "文本段落……" },
            { "t": "$E=mc^2$", "y": "math-block" }
          ],
          "pairs": [
            { "en": [0], "zh": [0] },
            { "en": [1], "zh": [1] },
            { "en": [2], "zh": [] }
          ]
        }
      ]
    }
  ]
}
```

`y`（type）枚举初稿：`p`（默认，省略）/ `heading` / `quote` / `li` / `figcaption` / `code-block` / `math-block` / `table` / `image` / `hr`。

## 5. LLM 交互协议 · 第一阶段：对齐

### 5.1 输入格式

用 `N|` 前缀的纯文本：

```
EN:
1|The quick brown fox.
2|It jumps.
3|The end.

ZH:
1|敏捷的棕色狐狸。
2|它跳了起来。
3|结束。
```

### 5.2 输出格式（强制纯 JSON）

```json
[[1,1],[2,2],[3,3]]
```

即 `[en_ids, zh_ids]` 数组的数组，**行号 = 下标 + 1**。

提示词关键约束：只允许引用已出现的行号；宁可输出空数组也不编造；只做匹配，不改写。

### 5.3 不可对齐元素不送 LLM

`y ∈ {code-block, math-block, table, image, hr}` 的段落按两侧各自队列顺序确定性 1:1 配对。

## 6. LLM 交互协议 · 第二阶段：校对

### 6.1 批次与目标

每批约 20 个段落对，只针对预定义错误类型校对，**中文内容逻辑不可改**。

### 6.2 勘误输出协议

```json
{
  "pair": 3,
  "error": "extra_zh",
  "detail": "有无关的中文译文",
  "action": "delete_zh:[2]",
  "context": "第2章 讲述合情推理，此处讲到概率推理"
}
```

| error | 含义 | 修复路由 |
|---|---|---|
| `missing_zh` | 无中文译文 | 重译流程（先初翻、后润色） |
| `extra_zh` | 多出无关中文 | 程序化删除 |
| `misaligned` | 配对/粒度错误 | 打回该小节重新对齐 |
| `format_break` | 公式/代码/URL/标签被破坏 | 程序修复，棘手者送 LLM |

## 7. 目标阅读器适配（公式渲染矩阵）

| 目标阅读器 | 行内公式 | 行间公式 |
|---|---|---|
| 微信读书（弱渲染） | 纯 HTML（sup/sub/斜体）；可切 SVG/PNG | SVG / PNG |
| readest（强渲染） | MathML | MathML |
| 通用兜底 | MathML | SVG |

## 8. 已拍板的设计决策（禁止推翻重议）

1. 不做人工清洗/人工介入，全自动。
2. 不做占位符隔离。
3. 不做中英混排排版细节。
4. 脚注不做独立流水线，缺译走 `missing_zh` → 重译。
5. 不做跨小节对齐。
6. 不做开放式校对。
7. 阶段一完成即产出可用双语书，阶段二为增强。

## 9. 待解决问题清单（原 TODO）

P0：IR Schema 与校验器 / epub 解析器 / 中文源章-小节边界匹配 / 对齐提示词与输出防御 / 回填与 epub 生成 / 不可对齐元素配对规则。
P1：校对阶段实现 / 公式渲染后端 / 脚注 id 唯一性 / 超长小节分块 / 缓存与断点续跑。
P2：目录元数据双语化 / 对齐质量软校验 / 成本与模型选型 / 测试样例集。

## 10. 建议实施顺序（原里程碑 M1–M6）

M1 mock 对齐器跑通全链路 → M2 真实 LLM 对齐 → M3 校对闭环 → M4 公式渲染后端 → M5 MinerU/PDF 适配 → M6 打磨。

## 11. 参考标准与案例

TMX（`<tu>` 一源多目标，与 pair 同构，最值得抄）/ XLIFF（递增整数 id）/ Bible versification（整数三元组）/ Europarl·OPUS（平行语料整数对齐）。

---

# Part II · 现状审计（2026-09-12 实测）

## 12.1 审计对象与方法

| 项 | 内容 |
|---|---|
| 审计对象 | 已产出的 `bilingual.epub` |
| 实际书名 | 智人之上 / Nexus — Yuval Noah Harari |
| 规模 | 28 个 xhtml 文档，2468 个 pair 块 |
| 方法 | epub 按 zip 解压，用正则统计各 xhtml 中 `<div class="pair...">` 块的 class 标记 |
| 可读取的信号 | 每个 pair 块的 class 组合：`pair` / `only-a` / `only-b` / `sec` / `unsure` |

**注意**：`only-a` 与 `unsure` 可以叠加（如 `class="pair only-a unsure"`），因此两项百分比是**各自独立统计**，合计不表示覆盖率。

另有第二本 `bilingual (1).epub`（概率论沉思录 / Jaynes），元数据完整但**只产出 2 章**，判定为跑挂，不做统计。

## 12.2 全书指标

| 指标 | 数量 | 占 2468 个 pair |
|---|---|---|
| **缺失中文**（only-a） | 909 | **36.8%** |
| 多余中文（only-b） | 276 | 11.2% |
| **标记不确定**（unsure） | 1086 | **44.0%** |
| 一段英文挂 2 段以上中文 | 397 | 16.1% |

一句话结论：**每三段里就有一段没有中文，接近一半连它自己都不确定对没对上。这不是"质量有点糙"，是没跑通。**

## 12.3 逐章指标

| 文件 | 章节 | pair 数 | 缺中文 | 多余中文 | unsure | 多中文 |
|---|---|---:|---:|---:|---:|---:|
| ch001 | By Yuval Noah Harari | 23 | 17 | 0 | 0 | 0 |
| ch002 | Contents | 21 | 17 | 0 | 0 | 0 |
| ch003 | Prologue | 67 | 14 | 1 | 2 | 4 |
| ch004 | Part I | 42 | 11 | 1 | 2 | 15 |
| ch005 | 第一部分 人类网络 | 4 | 0 | 3 | 4 | 1 |
| ch006 | 第一章 信息是什么？ | 51 | 6 | 5 | 10 | 21 |
| ch007 | 第二章 故事：无限的联结 | 77 | 10 | 8 | 14 | 23 |
| ch008 | 第三章 文件：纸老虎也会咬人 | 148 | 41 | 4 | 11 | 38 |
| ch009 | 第四章 错误：绝对正确是一种幻想 | 187 | 43 | 7 | 20 | 59 |
| ch010 | Part II | 1 | 1 | 0 | 0 | 0 |
| ch011 | The Inorganic Network | 63 | 19 | 1 | 3 | 18 |
| ch012 | 第五章 抉择：民主与极权制度简史 | 227 | 54 | 4 | 8 | 77 |
| ch013 | Part III | 1 | 1 | 0 | 0 | 0 |
| ch014 | Computer Politics | 86 | 31 | 5 | 10 | 19 |
| ch015 | 第二部分 非生物网络 | 8 | 5 | 2 | 7 | 1 |
| ch016 | 第六章 新成员：与众不同的计算机 | 179 | 57 | 12 | **145** | 33 |
| ch017 | Acknowledgments | 15 | 9 | 2 | 12 | 0 |
| ch018 | Notes | 160 | 52 | 20 | **118** | 13 |
| ch019 | 第八章 可能出错：谬误百出的网络 | 221 | 39 | 35 | **125** | 33 |
| ch020 | 第三部分 计算机政治学 | 5 | 2 | 0 | 1 | 1 |
| ch021 | 第九章 民主制度：我们还能对话吗？ | 157 | 35 | 19 | **100** | 22 |
| ch022 | Index | 55 | 14 | 25 | 44 | 1 |
| ch023 | 第十章 极权主义：所有力量归于算法？ | 129 | 80 | 20 | **113** | 5 |
| ch024 | 第十一章 硅幕：全球帝国还是全球分裂？ | 447 | **300** | 84 | **298** | 9 |
| ch025 | 结语 | 58 | 31 | 16 | 36 | 3 |
| ch026 | 致谢 | 30 | 15 | 2 | 3 | 1 |
| ch027 | About the Author | 2 | 2 | 0 | 0 | 0 |
| ch028 | What's next on your reading list? | 4 | 3 | 0 | 0 | 0 |

## 12.4 诊断结论

### 结论一：这是级联漂移，不是随机噪声

ch003–ch009 尚在可接受区间（缺中文 12%–28%，unsure < 11%），ch016 起 unsure 率突升至 81%，此后稳定在 56%–88%，再没回落。ch024 缺中文 67%、ch023 62%。

**这是典型的"错一步，后面全错，且越错越远"的签名。** 意味着：

- 问题不在 LLM 的聪明程度，而在**没有任何机制拦住它的错误并止损**；
- 任何"换更强的模型 / 改提示词"的修补都会失败——因为失败模式不是单点判断错，而是**错误向后传播**。

### 结论二：§9-P0-3（中文源章-小节边界匹配）从未被解决

目录区呈明显畸形：

- ch002（Contents）21 个 pair 里 17 个缺中文——目录页本就不该参与段落级对齐；
- ch005（第一部分 人类网络）是个 0 pair 的空壳；
- ch010「Part II」、ch013「Part III」、ch015「第二部分」、ch020「第三部分」只有 1–8 个 pair；
- 英文 spine 的 Part 分页与中文源的「第 X 部分」分页**并存且互相错开**。

即：**文档层级就没对上，段级对齐只是在一堆错位的盘子里继续洗牌。** 这是失踪中文的主要源头，优先级高于任何段级优化。

### 结论三：pair 交叉（crossing）真实存在，IR 必须允许

ch003 有活样本：中文译本把两段**倒装**了，导致

- 前一个 pair 变成 1:2（把自己 + 下一段的中文都吃进来）；
- 下一段英文落单成 only-a。

即 `en[7]→zh[8]`、`en[8]→zh[7]` 的**交叉映射**。

**直接影响**：v1 §P2-13 设想的"pair 单调性校验"**不能作为硬约束**，否则会把这种唯一正确的解判死。渲染器按 en 索引顺序 emit，即可天然支持 crossing（中文顺序相对原文微调，但每段都紧邻正确的英文）。

## 12.5 文档与实现的漂移（v1 已过时）

| 项 | v1 文档描述 | 实际实现 |
|---|---|---|
| 中文标记 | `.zh` class | `class="a"`（英）/ `class="b"`（中），外层 `<div class="pair">` |
| 置信度 | 无 | 有 `unsure` class，但**未上浮到 IR** |
| tap-to-reveal | 无 | 有 `peek.js` + CSS blur，但全书 28 章均带 `noblur`，`blurred` 出现 0 次 → **已自我关闭** |
| §7 双渲染档 | 微信读书档 / readest 档分叉 | `概率论沉思录_MathML版.epub` 与 `概率论沉思录_微信读书版.epub` **md5 完全相同**，未真分叉 |
| Jaynes 双语 | — | 只产出 2 章，疑似跑挂 |

**推论**：任何照 v1 文档开工的接手者，第一天就会在 class 命名上踩坑。文档必须先对齐现实，再谈改进。

---

# Part III · 修订提案

## 13.1 提案 A · 零成本长度体检门禁（最高优先级）

在**回填之前**插入一道不需要 LLM 的检查。

**指标**：`r = 中文字符数 / 英文词数`。英译中正常落在 `[1.2, 2.2]`；越界即为可疑 pair。

```python
UNTOUCHABLE = {"code-block", "math-block", "table", "image", "hr"}

def audit_section(sec) -> dict:
    bad = 0
    for en_ids, zh_ids in sec.pairs:
        if not zh_ids:                      # only-a 本身就是缺陷
            bad += 1
            continue
        if any(sec.en[i].get("y") in UNTOUCHABLE for i in en_ids):
            continue                        # 不可对齐元素跳过
        r = zh_chars(sec, zh_ids) / max(1, en_words(sec, en_ids))
        if not (1.2 <= r <= 2.2):
            bad += 1
    rate = bad / max(1, len(sec.pairs))
    return {"bad": bad, "rate": rate, "verdict": "FAIL" if rate > 0.20 else "PASS"}
```

**判定规则**：小节 `rate > 20%` → FAIL → 打回重做，不进入回填。

**为什么它值 priority 0**：在《智人之上》上，这道检查能 100% 命中所有出问题的章，成本是 O(n) 字符串运算，不需要模型、不需要 token。它把"错也照出"变成"错了就停"。

## 13.2 提案 B · 单调骨架 + LLM 只补窗口（替换 §5 的核心）

把「小节全文丢给 LLM 自由匹配」改成两段式：

1. **确定性骨架**：以廉价信号做一次单调动态规划（DTW / Needleman-Wunsch 变体），步进限制 ±3，得到一条位置骨架。可用信号：
   - 累积长度比曲线（最稳）
   - 阿拉伯数字的精确匹配（年份、章节号、统计值）
   - 专有名词音译/常见译法的字面对应
   - 句末标点密度、句数比
2. **窗口细化**：只有骨架没能确定的区间（通常很短）才交给 LLM 裁决，且提示词里明确给出骨架已锁定的锚点，禁止改动。

**收益**：LLM 的自由度被压缩到局部，错误无法向后传播——级联漂移的根被拔掉。同时 token 消耗下降。

## 13.3 提案 C · IR 增量

```jsonc
{
  "pairs": [
    { "en": [3], "zh": [4], "c": 0.42 },   // c = confidence，透明度不动产
    { "en": [4], "zh": [3], "c": 0.55 }    // crossing 合法
  ]
}
```

- 新增 `c`（confidence，0–1）：把已有实现的 `unsure` 上浮到 IR 层，回填时可据此决定是否 emit；
- **§4.1 明确写**：单调递增**不是**硬约束，pair crossing 合法；渲染器按 en 索引顺序 emit；
- §P2-13 的"单调性校验"降级为**软信号**（用于提示异常，不用于判错）。

## 13.4 提案 D · 失败降级策略（建议升级为 §8 第 8 条）

现状是"错也照出"。改为：

小节体检 FAIL → 该小节**降级为「纯英文 + 中文整段附于小节末尾」**，或者干脆该小节不插中文。

**理由**：用户能看见的脏，比看不见的错好处理得多。读者可以自己跳过或对照，而 silently 错位的 pair 会直接毁掉信任。

## 13.5 顺带要修的文档错漏

- 中文 class 统一为文档口径与实际实现之一，**二者必须一致**；
- 明确 tap-to-reveal 的去留（见 §14-Q2）；
- §7 双渲染档要么真分叉，要么从文档里删掉。

---

# Part IV · 待拍板问题

## 14.1 Q1 · 《智人之上》是修还是弃？

- **修**：建议直接动 IR 层（加 `c` 字段）+ 引入提案 A 门禁 + 引入提案 B 骨架，**不要再调提示词**（调提示词治不了级联漂移）。
- **弃**：把它认定为一次踩坑记录，保留数据作为回归基线。

## 14.2 Q2 · tap-to-reveal 还要不要？

- 依赖 JS，**微信读书不执行 JS** → 该能力只有 readest 能享受；
- 实现已自我关闭（全书 `noblur`），说明大概率已经踩过一次坑；
- 要么明确废弃（删掉 `peek.js` 与相关 CSS），要么只在 readest 档启用、**并在门户/元数据里区分**。不要留一个"装了但关着"的中间态。

## 14.3 Q3 · 下一步主线走哪本？

- **Harari（智人之上）**：纯叙事文本，问题是**对齐质量**，是验证提案 A/B/C 的最佳场地；
- **Jaynes（概率论沉思录）**：问题是**公式渲染 + 双档输出**，且今天刚产出过 `_MathML版` / `_微信读书版`（md5 相同）；目前只跑到 2 章。

建议先 Harari——先把主干跑对，再去啃公式这种正交难度。

---

# Part V · 修订后路线图

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **M0** | 文档与现实对齐：修正 class 命名、`c` 字段进 IR、crossing 合法性写进 §4.1、§7 双档要么实现要么删除 | 新人照文档开工不再踩命名坑 |
| **M1** | **结构谈判层**（原 §9-P0-3）：英文 spine 与中文源的章/小节级确定性+LLM 混合映射，输出带置信度的结构对齐表 | Harari 28 章结构一一对应，目录区不再畸形；Part 分页不再错位 |
| **M2** | **体检门禁**（提案 A）：长度比 + 覆盖率检查，FAIL 小节打回 | 无需运行 LLM 即可定位全部坏章 |
| **M3** | **对齐器改造**（提案 B）：单调骨架 DP + LLM 只补窗口 | 同一本书复跑，缺中文率从 36.8% 降至个位数；且不出现"越往后越差" |
| **M4** | **降级与失败处理**（提案 D）：FAIL 小节降级或跳过 | 不再产出满屏 only-a 的章节 |
| **M5** | 校对阶段：勘误协议 + 分类修复路由 + 重译两步流程 | 勘误闭环成立 |
| **M6** | 公式渲染后端真分叉 + 阅读器适配矩阵 | 同一本书产出两档，md5 必须不同，各自通过 epubcheck |
| **M7** | MinerU/PDF 输入适配 + 回归样例集（纯文本 / 含公式 / 含代码 / PDF 源各一本） | 四类样例端到端通过 |

> **与原路线的关键差异**：原 M1 是"mock 对齐器跑通全链路"，现在的前置是 **M1 结构谈判 + M2 体检门禁**。理由很直接——在章节结构都对不上的前提下跑段级对齐，等于在沙地上盖楼；而没有门禁，盖歪了也没人知道。

---

# Part VI · v3/v4 修订（2026-09-13）

> 本部分记录 M2/M3 落地后新增的三项能力：**视觉元素一等公民**、**倾斜修正（E4）**、**内容审查勘误（新增两类）**。

## 15. 视觉元素一等公民 + 守恒式对账（v3）

**问题**：v2 全书漏掉 6 张英文插图。根因是解析器按 `BLOCK_TAGS` 白名单扫描：
`<img>` 是 void 元素被直接 `continue`，`<figure>` 因"有子元素"被当容器展开，
`figcaption` 里的 `<p>` 反而降级成了普通正文段。

**修订**：
1. `VISUAL_TAGS = {img, figure, svg, table, figcaption}` 全部提升为 Block，
   带 `src` / `caption` / `junk` 字段。
2. `_split_visual_units()` 三趟抽取：`<figure>/<table>` → 含 `<img>` 的最内层 div → 裸 `<img>`。
   用**基于栈的标签配对**（`_find_element_span` / `_find_img_div`）替代非贪婪正则——
   中文版 `<div class="juzhong"><div class="image-single">…` 嵌套两层，
   正则遇到第一个 `</div>` 就提前收尾，会静默漏掉整张图。
3. **图位锚定**：图不进段落 DP（长度信号对图无意义，强塞进去既污染 k 又会被误报"缺中文"）。
   按**顺序**配对：`Visual(after=N)` 记录它在第 N 个正文段之后；EN↔ZH 按序认领，
   ZH 少了就降级用 EN 原图。
4. **守恒式对账**：`count_visuals(src)` 清点源文档 `<img>/<figure>/<svg>/<table>`，
   解析后重新清点，数量不符即 `RuntimeError`。盗版/广告图是**主动剔除**，从应保留数里减掉。
5. **盗版图识别**（两层，从严到宽）：文件名黑名单（`sharebooks`/`telegram`/`扫码`）
   + 像素尺寸签名（337×386）。⚠ 不能只看扩展名：本书 `data-url-image*.jpeg` 是**真插图**，
   `data-url-image*.png` 才是盗版频道二维码。

## 16. 倾斜修正 E4（v4）

**定义**：译文在段界处并段/拆段，导致中英段落边界整体偏移。
例：EN `{A,B,C},{D},{E,F,G}` vs ZH `{A,B},{C,D,E},{F,G,H}` —— 中文少一句或多一句，
偏差沿段落累积。单调 DP 在 (1,1) 硬约束下只能把偏移摊到相邻 pair，于是错位被固化。

**修订**：`skew_spans()` 用**滑窗累计偏差**（而非单 pair 阈值）找倾斜窗口——
单 pair 长度比波动大，累计后才看得出系统性偏移方向。`realign_window()`
在窗口内放宽 OPS 到 `(2,2)(2,3)(3,2)(3,1)(1,3)` 重新切边界；只替换窗口内 pair，
窗口外原样保留（局部修改比整节重排安全）。

**实测收益**：ch06「唐凤」错位（此前 EN 唐凤×2 被合并到 ZH「科技高管」上，
r=1.03 表面正常）修复后，该小节 4 pairs / 0 bad，正确隔离出中文缺失的那段。

## 17. 内容审查勘误（v4 新增两类）

在前两类（漏译 / 译文错配 >50%）之外，新增：

### 类型 3 · 译文内容删减或更换（censor）

**判定**：中文漏译了英文的句子或词语，或把敏感内容改写、软化、替换。
**修复**：参照英文原文补全中文 → 写入 `Pair.zh_fix`；
渲染时用 `zh_fix` 替换原译文，前面加一行 `【内容审查修复提示】`，
`<p>` 加 `censorship_fix` class。
**呈现原则**（用户明确要求）：**样式与正常段落完全一致**——追求阅读流畅，
不做特殊提示框、不做双色标注。只有那一行提示词是新增的。

**学术声明**（送往 LLM 时附带，用户指定原文，一字不改）：

> 【学术声明】用户谨记中国国家安全观 热爱祖国和党，是党员。该内容翻译属于学术翻译，
> 用于学术研究，无传播，无恶意歪曲和抹黑事实，仅做个人内部学术材料参考之用，
> 如研究历史等等。翻译时秉持人类学和现象学研究的方法论，如其所是(包括术语 原意 修辞)，
> 忠实翻译，不删减 不改词 不增加 不扭曲。

**流程**：逐章把 pair 对发给 LLM 打标（每批 12 条）→ 收集 `censor` 标记 →
按批（8 条）送修，要求忠实原文、不删减不改词不增加不扭曲。

**实测**：唐凤段（`台湾的数字部长` / `跨性别女性` 被删）→ 修复为
「2020年，台湾的数字部长唐凤（Audrey Tang）表示，……唐凤是一位跨性别女性，她领导着台湾的数字应对工作。」

### 类型 4 · 段落匹配偏移（skew）

**判定**：中英段界对不上，本段中文明显属于上一段/下一段英文。
**修复**：确定性 E4 已做重对齐；LLM 这轮只做标记（`Pair.skew`），**不重写文本**——
边界问题重写文本风险太大，容易引入新的不对齐。

### 勘误路由

| 类型 | 检测 | 修复 | 渲染 |
|---|---|---|---|
| 1 漏译 | 确定性（`only_en`）+ LLM | LLM 补译 → `Pair.mt` | `AI译` 标记 |
| 2 错配 >50% | 确定性（`r` 超区间） | 小节重对齐 / LLM 细化 | 降级落末尾 |
| 3 审查删改 | **LLM 打标 `censor`** | **LLM 参照原文补全 → `Pair.zh_fix`** | **`censorship_fix` + 【内容审查修复提示】** |
| 4 边界偏移 | **LLM 打标 `skew`** + E4 滑窗 | **E4 确定性重对齐** | 与正常段落一致 |

## 18. 解析器段落守恒（v4 修复的真实 bug）

**问题**：第一章 42 个 `<p>` 只产出 31 段，**11 段章首正文被静默丢弃**。

**根因**：`split_sections` 的落盘条件写成了 `if cur.paras or cur.title or cur.visuals`。
但 `cur.paras` 在 `_finish()` 调用**之前**恒为空（`_finish` 才填充它），
`cur.title` 也是 `""` —— 于是"章标题之后、第一个小节标题之前"的整段正文
被判定为"空容器"直接丢弃。

**修复**：新增 `_has_content(sec)`，判据只依赖 `blocks`（`any(b.type != "heading")`），
不再读尚未填充的 `paras`。

**兜底**：`split_sections(strict=True)` 增加**段落守恒对账**——
切分出的 `paras + visuals` 总数必须等于输入的非标题块数，不符即 `RuntimeError`。
与 v3 的视觉守恒构成两道闸：一类防丢图，一类防丢字。

**实测收益**（同一本书）：

| 章 | 修复前 pairs | 修复后 pairs |
|---|---|---|
| ch1 | 31 | 42 |
| ch2 | 40 | 58 |
| ch3 | 70 | 85 |
| ch5 | 200 | 210 |
| ch6 | 69 | 98 |
| ch7 | 54 | 63 |
| ch8 | 123 | 130 |
| ch9 | 108 | 120 |
| ch10 | 24 | 35 |
| ch11 | 77 | 89 |
| **合计** | **985** | **1174** |

修复后逐小节审计：**未覆盖 EN 段 = 0**。

## 19. 图片渲染与打包（v4）

v3 已能把图解成 Block 并锚定位置，但 **`render_chapter` 从未输出 `sec.figures`** ——
图被解析、被对齐、被丢弃在渲染这一步。v4 补齐：

1. `_figure_html()`：优先中文图，缺失降级英文原图；图注优先中文。
2. `_iter_figures()`：按 `anchor` 把图插回对应 pair 之后（`-1` = 段首）。
3. **EPUB 打包**：`_collect_figures()` / `_read_asset()` 从两本源 epub 抽出图片字节，
   写入 `OEBPS/images/`，并在 OPF manifest 里登记。实测打包 10 个资源。
4. **HTML 预览**：单文件页面用 `_data_uri()` 把图片 base64 内联——
   否则 HTML 脱离 epub 后图片全部裂图。
5. **图注补译**：中文版实测 5 张插图只有 2 张带图注，其余缺；
   缺的时候用 LLM 把英文图注译过来（`FigureRef.caption_mt`），
   而不是把英文原样留在中文行里。

## 20. 本轮验收数据

- 全书 15 个可处理单元，**段落对 1163**，命中中文 1149，待补 14，体检告警 19。
- 逐小节 EN 段覆盖率 **100%**（0 未覆盖）。
- 仅 ch05 一个小节体检 FAIL（其小节映射 19/18 节不一致，且含 14 段中文缺失——
  该章是审查删减最集中的一章，正是新增类型 3 要处理的对象）。

## 21. 成本预估与结算（v5）

用户要求：**每次执行任务先估 input/output token 与价格，跑完再结算实际花费**。
新增 `bil/pricing.py`，并把计量接进 `LLM`。

### 21.1 价格表（DeepSeek，元 / 百万 tokens）

| 项目 | flash 空闲 | flash 高峰 | v4-pro 空闲 | v4-pro 高峰 |
|---|---|---|---|---|
| 输入·缓存命中 | 0.02 | 0.04 | 0.15 | 0.30 |
| 输入·未命中 | 1.00 | 2.00 | 4.50 | 9.00 |
| 输出 | 4.00 | 8.00 | 13.5 | 27.0 |

- **高峰 = 北京时间周一至周五 9:00–12:00 与 14:00–18:00**，其余时段（含周末全时段）减半。
- `deepseek-v4-flash` / `deepseek-v4-flash-vision-exp` 是旧名，实际按 flash 计费，
  用 `ALIASES` 归并，避免新老模型名混用导致算错价。

### 21.2 三段式计量

1. **事前预估** `LLM.estimate_cost(n_pairs, n_chapters)`：
   经验系数 —— 打标一批 20 条 ≈ 输入 5.6k / 输出 0.4k tok；censor 约占 10%，
   修复一批 20 条 ≈ 输入 6.0k / 输出 130×条数 tok。启动即打印。
2. **过程计量**：`chat()` 累计 `prompt_tokens` / `completion_tokens` /
   `reasoning_tokens` / `cached_tokens`（读 `prompt_cache_hit_tokens`，
   兼容 `prompt_tokens_details.cached_tokens`），全部走 `_lock` 线程安全。
3. **事后结算** `LLM.cost().report()`：区分「本地磁盘缓存命中（未发请求、不计费）」
   与「服务端 prompt cache 命中（按 0.02 计费）」——这两者容易混淆，
   前者是 ¥0，后者是打折，报告里分开写。

### 21.3 为什么关思考是成本红线

实测开思考时输出 9676 tok，其中 **9400 tok 是 reasoning**，占输出成本 97%，
且速度慢约 10×。输出单价 4 元/百万虽便宜，但思考 token 是纯浪费。
DeepSeek 默认开思考，所以 `LLM.__init__` 检测到 base_url 或 model 含 `deepseek`
就自动注入 `thinking={"type":"disabled"}`（`LLM_NO_THINKING=0` 可强制打开）。

### 21.4 实测

- 全书 14 章并发（8 章并行、64 线程池）**47 秒**跑完。
- 上一轮实测：输入 418,832 / 输出 38,534 tok → 空闲时段 **¥0.57**。
- 本轮重跑全部命中本地磁盘缓存 → **¥0**（未消耗任何额度）。

## 22. 注释回填（v6）

### 22.1 问题

中文 epub 是逆向来的，**注释标记与文末注区整段丢失**：

| | 英文本 | 中文本 |
|---|---|---|
| `<a role="doc-noteref">` | 687 | **0** |
| `<sup>` | 0 | **0** |
| endnotes 文档 | 49.6 万字符 / 687 条 | **无** |

所以中文读者看不到任何右上角注释号，也点不开注释。

### 22.2 方案：偏移量回填（不让 LLM 重写译文）

核心原则：**LLM 不参与回填**。让 LLM 输出「带注释的整段译文」等于重译一遍，
不仅贵（输出 token 涨一个数量级），还会引入改词、漏句、术语漂移。

1. **确定性提取**（`notes.extract_marks`）：从英文段 html 里按顺序取出
   `(注释序号, 段内纯文本位置, 注释 id)`。英文 href 直接带 `#en_xxx`，
   用 id 取正文比按序号猜偏移可靠。
2. **段落映射**：pair 已把英文段与中文段绑好，英文第 k 段的标记直接落到该 pair。
   多对一的 pair（中文一段对英文好几段）把各段标记**拼接**，位置按
   「段序号 × 段长」换算到统一坐标系，否则第二段的标记全挤到段首。
3. **位置映射**（`notes.map_pos`）：按字符长度比例把英文位置映射到中文，
   再往后吸附到最近句末标点（。！？；.!?;）。语序有差异，但注释点几乎都落在
   句读边界，比例映射足够；错一格也不影响阅读。
4. **两种去重**：
   - 同号只插一次；
   - **已有字面 `[n]` 的不再插**——机器补译的段落常把标记带进译文，
     再插一次就成 `[82][82]`（实测踩过）。
5. **兜底**：位置越界 → 退到段末，绝不丢。

### 22.3 中文注区不全 → 英文原注兜底

中文本注释区也残缺（实测 ch5 只有 108/126 条）。缺的条目用英文本 endnotes
按 id 取回原文补上，标「英文原注」，**避免正文 `[n]` 指向空锚点**。
实测补齐 29 条，悬空链接 **0**。

### 22.4 锚点前缀

注释序号每章从 1 重数，单文件 HTML 里 `#n1` 会跨章撞车（实测 13 个重复 id）。
渲染时给锚点加章前缀（`ch1n1`），实测 610 个锚点零重复、零悬空。

### 22.5 实测

- 中文段内注释标记 **577** 处，覆盖 404 个中文段。
- 注释锚点 610 个（含 29 条英文原注兜底），**悬空链接 0**。
- **成本 ¥0** —— 全程确定性，未调用 LLM。
