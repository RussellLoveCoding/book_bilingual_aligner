"""解析结果的磁盘缓存 —— 让「非 LLM 的活」在热跑时落到 1 秒以内。

为什么需要：解析一本 epub 要读遍每个 spine 文档 + CSS（ml 中文那本 94MB，
单遍 2.4s；四本书冷跑合计 ~16s）。而这类结果**只跟源文件有关**，
源文件没变就没必要重算。缓存放 `tools/.cache/parse/`（和 `.cache/eq`、
`.cache/llm` 一个待遇：**永不删**）。

键 = (绝对路径, mtime_ns, size, 版本号, 参数…)：源文件一改 mtime 就变，
自动失效，不会拿到脏数据。`_V` 是**格式版本号** —— 改了上游解析逻辑
（`epubparse` / `titlesrc` / `sectmine`）一定要 +1，把旧缓存全废掉。

⚠ 只缓存**纯函数结果**（同样的输入必得同样的输出）；调用方**只读**，
不要改返回值（改了会污染后续调用）。
"""
from __future__ import annotations

import hashlib
import json
import os
from pathlib import Path

_V = 29  # v29（2026-09-19 §6.43）：**中文源书内联样式垃圾清洗**。
         #     用户报「版面感觉很乱」。查下来**不是对齐问题**，是中文译本
         #     epub 转制时带了一堆对本流水线毫无意义的内联样式，被忠实搬运：
         #       《机器学习实战》中文源：<sub> 31289 · <span> 12043 ·
         #       font-size 出现 9120 次。
         #     最恶劣的是**逐字拆下标**——用
         #       <span style=""><sub>V</sub><sub>o</sub><sub>t</sub>…</span>
         #     表示 `VotingClassifier`：共 2657 个串、28536 个 <sub> 标签
         #     ＝ 全源书 sub 的 **91%**。阅读器里每个字母都掉到基线下，极乱。
         #     ⇒ `build._tidy_inline` 幂等清洗：① 同标签内连续逐字 sub/sup
         #       合并成一个；② 剔掉 style 里的**纯排版声明**（font-*/行高/
         #       字距/vertical-align/裸字体名），**保留** color/background/
         #       text-decoration（那是有意强调，删了丢语义）。
         #     ⚠ 边界：公式走 `<i>/<sup>/<sub>` 是 latexrender 正常产物，
         #       单个下标**不受影响**（判据要求同标签内 ≥2 个连续单字符）。
         #     ⚠ 这是**渲染层**清洗（`_zh_math` 咽喉），产物级生效；
         #       仍在 v29 是因为 `Section.zh_paras[].html` 会被 `_zh_math`
         #       统一过滤，属于「产物内容改变」→ 旧缓存里的 html 形态已不同。
         #     ⚠ 回归边界：清洗是**无条件幂等**的，对 prob/think2/nexus/ml
         #       一律只减不增；已用产物级回归确认四本书指标无变化。

         #     《机器学习实战》英文原版（O'Reilly DocBook）把提示框写成
         #       <div data-type="note"><h6>Note</h6><p>正文…</p></div>
         #     全书 **266 个**（tip 127 / note 81 / warning 57 / caution 1）。
         #     `epubparse` 原有设计「非小节容器里的 heading 降级成 para」是对的
         #     （不降级就会被切成 30 个假小节、bad 80%），但降级**降进了段落流**：
         #     中译本把提示框摊平成普通段落、没有这个标签（实测中文版「只有标签、
         #     无正文」的独立段落数 = 0）⇒ ① 判 1:0 变孤儿 pair；② DP 抓上一条
         #     公式的残渣填中文格（`zh=<i>，</i><i>k</i>` 配 `en=Note`、
         #     `zh=23` 配 `en=Warning`）。
         #     ⇒ 新增块类型 `box_label`（判据见 `epubparse._box_label_of`：
         #       祖先容器声明的类型 ↔ 文本 fullmatch，**纯结构、零词表**），
         #       `Block.is_para` 为假 → 不进 DP；保留在 `Section.box_labels`
         #       供渲染层当提示框标题输出（内容不丢）。
         #     ⚠ 回归边界：prob / think2 / nexus 的提示框容器数**均为 0**
         #       ⇒ 本改动对既有三本回归书结构上不可能命中。
         # v27（2026-09-18 §6.37）：**补译前哨** —— ≤2 词的残片不发 LLM。
         #     用户截图：公式 (22.70) 上方出现 AI 补译的「22. 传播理论导论」。
         #     英文原文那里是 `<p class="noindent1">and in</p>`（公式图把一句话
         #     劈开后掉下来的两词残片），中文版并进了下一段 → 判成 en-only →
         #     走补译 → 模型拿到无信息残片 + 无关上下文 → **凭空编出一个书名**。
         #     实测（p21 全书 766 个 AI 补译 pair）分类：
         #       索引条目（`词, 页码`）366（补译有意义，**豁免**）
         #       短残片（≤6 词、无句末）103  ← 风险集中
         #       其余（正常缺译段落）297
         #     残片词数分布 {1:16, 2:8, 3:34, 4:26, 5:12, 6:7} —— 1~2 词没有
         #     任何可翻译内容，产出不是噪声（`or`→「或」）就是幻觉。
         #     ⇒ `pipeline._too_short_to_translate`：≤2 词且无句末标点且
         #       非索引条目 → 跳过补译（该段只显示英文原文，读者仍看得懂）。
         # v26（2026-09-18 §6.36 续三）：**伪标题判据换锚点**。
         #     v25 用「英文前一句是否未收尾」判残片 → 实测**误杀真标题**：
         #     `历史题外话` 的前一句恰好是公式引导语（`(4.64) reduces to`），
         #     被降级丢弃 —— 但英文原版在**同一位置有真标题**
         #     `<p class="h2" id="h2_4.6.1">4.6.1 Historical digression</p>`。
         #     ⇒ 主判据改为**英文侧同位置有没有登记标题**（`en_heads_at`）：
         #       有 → 真标题（历史题外话 / 无差别是基于知识还是无知？）；
         #       没有 → 英文那边是散文句 → 残片（验几率是 / 第三个观测值…）。
         #     这个判据零词表、跨书成立，且复用已有的标题登记表。
         #     「英文未收尾」降级为**兜底**（仅当英文侧确无标题登记时才用）。
         # v25（2026-09-18 §6.36 续二）：**伪标题残片的泛化判据**。
         #     p20 实测：`_MIN_SLACK=3` 之后 judge 触发降到 9 节（收口成功），
         #     但 h4.st 从 65 → 68，多出 3 个。逐一核对：
         #       `无差别是基于知识还是无知？`  ← 真标题（18.11.1 的中译），正确 ✔
         #       `验几率是`                    ← 残片（公式图劈开句子），伪标题 ✘
         #       `第三个观测值的预测概率密度为`    ← 残片（同上）✘
         #     两个残片**既不以连接词开头、也无句末标点**，`_FRAG_START` 词表
         #     （`因为/所以/面的/的…`）拦不住 —— 这是「词表式判据」固有的
         #     按书调参性质（换书就冒新词）。用户明确反对。
         #     ⇒ 改用**英文侧句子性**（零词表、跨书成立）：zh-only pair 在源
         #     文里的前一个英文块若是「>60 字符且未以终结符收尾的散文句」，
         #     则本短中文段是它的续写残片。实测三条（`Given y, the possible
         #     values of α are…` / `For example, Jeffreys (1932) gave…` 否决；
         #     `…inductive reasoning.` 已收尾 → 保留）全部符合预期。
         # v24（2026-09-18 §6.36 续）：**信号量闸门**（`align._MIN_SLACK=3`）。
         #     v23 的「成簇 + 上限=slack」把单次爆炸压住了（n=81 → 0）,
         #     但实测埋点（`BIL_JUDGE_TRACE=1`）显示**仍有 66 个小节进闸门**、
         #     41 个产生候选、共咨询 70 段 / 弃 43 段（设计预期 = §1.5 的 8 段）。
         #     分布一拍即明：**slack=1 有 49 个**（占 74%），slack=2 有 8 个 ——
         #     `slack=1` 几乎全是良性（小节末多一段中文 / 标题只算一侧 /
         #     跨小节碎片），拿它去问 LLM 等于「拿必然噪声当疑点」→ 实测 70%
         #     被回 N → 无故丢一段中文。
         #     ⇒ 新增闸门：`slack >= _MIN_SLACK`（3）才进 LLM。真正的
         #     「图写成散文」是**成片**的（§1.5 = 8 块、§7.2 = 9 块），
         #     不会恰好 1~2 句。实测 66 → **9** 个。
         # v23（2026-09-18 §6.36）：**§6.35 泛化机制的收口** —— 修 p18 的
         #     「LLM 盲判爆炸」（该书级事故，非按书调参）。
         #     p18 实测：`judge_zh` 全书触发 **91 次 / 弃 207 段**（设计预期 =
         #     §1.5 的 8 段），其中 2 次 `n=81 / drop=49 / n_vis=0`（该小节
         #     **一张图都没有**，LLM 拿不到「英文侧是图」的证据 → 裸文本瞎猜 →
         #     一次丢 49 段译文）→ ch31 参考文献出现**重复段**
         #     （`Shaw, D. (1976)` / `Siegmann, D. (1985)` 各两遍）、
         #     `dbg_qa` 16→17 类、漂移链 37→40。
         #     根因：闸门太宽（`slack>0` 即放行）+ 候选无上限（B 判据在
         #     条目式小节标到 81 个）。收口三闸（全部零形态知识、可跨书迁移）：
         #       ① `probe_uncertain_zh` 反过来先问 `slack`：`slack<=0` 直接空返回；
         #       ② **候选必须成簇**：只留最长连续候选串（§1.5 的 8 块是连着的；
         #          条目式小节的独有段是散开的 → 那是「中英不同构」，另一类问题）；
         #       ③ **候选上限 = slack**（中文多几块就最多怀疑几块）；
         #       ④ pipeline 新增闸门：**本小节必须有图表**（`en_figs` 非空），
         #          没图就别问 —— 本机制唯一的语义依据就是「英文是图/公式」。
         #     ⚠ 保留 §6.35 的判据本身（它是对的，只是闸门太松）。
         # v22（2026-09-18 §6.35）：**不确定窗口 → LLM 块级裁决**（泛化路径）。
         #     用户 2026-09-18 定调：「不要老是要按书调参吧？能否做成一个泛化
         #     的解决方案？适配各种电子书。它没必要说针对这个书的具体各种
         #     场景来写判据。」
         #     前三条吸附（§6.30 枚举块 / §6.31 短定义 / §6.33 脚注）都是把
         #     **某本书的排版形态**烧进了代码；§1.5 的「幂等性：$\left\{
         #     \begin{array}…」是第四个形态，三条全不命中。
         #     本节改为：`align.probe_uncertain_zh` 在 k 网格上采样，找出
         #     **DP 处置不稳定**的中文段（一会儿判独有、一会儿判有对应）=
         #     DP 举手说「我没把握」→ 只在这个窗口里问 LLM「英文侧是散文
         #     还是图」（llm.judge_zh_blocks，块级 drop/keep）。
         #     ⚠ 形态正则**保留**为「零成本快速通道」（命中即解决、不花钱），
         #     LLM 只兜住正则没覆盖的。触发有双重闸门（slack>0 + 真找到
         #     不稳定段），全书实测只有 §1.5 触发 1 次请求。
         # v21（2026-09-18 §6.34）：**修「英文脚注段吸附」漏接线导致的整章丢失**。
         #     §6.33 摘出脚注后 `a_paras` 被裁短，但 LLM 分支的
         #     `_pairs_from_map(..., peel_en=...)` 只传了 `_peel_en_g`、
         #     **漏了 `_peel_enote_g`** → 脚注段被重新拉回段流 → 局部下标按
         #     未裁剪段数编号 → `_metrics` IndexError → `_run_parallel`
         #     的 `except Exception` 静默丢章（前言章实测：Acknowledgments
         #     EN6→3，成品里**序言以下整章蒸发**）。
         #     修法：`_peel_en_g |= _peel_enote_g`；另加两道防线（_metrics
         #     越界显式抛 + run_book 失败章清单/拒绝构建）。
         # v20（2026-09-18 §6.33）：**英文脚注段吸附**（align.peel_en_notes）。
         #     英文小节末尾挂着原版脚注（`[1] In his presentation…`），中译本的
         #     注区在**章末**、不在本小节内 → 输入变成 EN8:ZH6。DP 实测：
         #       e0↔z0 e1↔z1 e2↔z2 e3↔∅ e4↔z3 e5↔∅ e6↔[z4,z5] e7↔∅
         #     即**脚注 e6 吞了 z4+z5**（c=0.9, r=1.007 看着很"合理"），把
         #     e3/e5 挤成孤儿 → AI 补译 → 同一句中文两遍（用户截图 §3.11.1）。
         #     ⚠ 关键实测：**同一小节去掉脚注（6:6）在任何 k 下都给出全 1:1
         #     正确解**；含脚注（8:6）则 k=1.1~1.7 全错、只有 k=2.0 才对。
         #     ⇒ 脚注是纯噪音，摘掉即修复，**不需要调任何常数**。
         #     摘出的段进 `res.notes_en`（原本是死字段），渲染层按英文原版
         #     版式单独列「英文原注 / Notes (original)」（铁律 10）。
         #     判据仅认 `[n]` 方括号强形态 —— `(n)`/`n.` 在正文里同样常见
         #     （Chapter13 `(1) Transitivity: …` 是决策论公理），不许单独作
         #     判据，否则重演 §6.30 第一版「误伤真·正文枚举」。
         # v19（2026-09-18 §6.31）：**英文短定义段吸附**（align.peel_en_defs）。
         #     英文原版把一组定义排成**多个独立段**（`A ≡ it will start to
         #     rain by 10 AM at the latest;` / `B ≡ the sky will become
         #     cloudy before 10 AM.`），中译本把它们**合成了一个
         #     `\begin{array}` 公式块** → 中文侧少块、英文段落单 → DP 判 1:0
         #     → 触发 AI 补译，而补译把 `≡` 抄成了 `≩`（用户截图
         #     「B ≩ 上午10点之前天空变阴。」）。
         #     判据同时修正两处（都是 2026-09-18 实测推翻的）：
         #       ① `_en_def_key` 由「无句末标点」改成**位置式**（段首紧凑符号
         #          紧跟 ≡/=）：旧判据对 `A ≡ …;`（分号）命中、对
         #          `B ≡ ….`（句号）漏判 —— 同族定义行只摘一半；且旧判据
         #          全书 30 条命中里 18 条是 `and the relation dv = dF(…)`
         #          这类句子碎片。新判据 19 条**全部是真定义行**。
         #       ② 判据③ 改成查**中文侧 visual**（原先把英文图位和中文段序号
         #          两个坐标系混比，纯属巧合能对）。
         #     实测全书命中 8 族 / 16 段（§1.1 §3.1 §4.2 §4.4 §8.11 §13.8
         #     §18.5 §18.15.2），全部双侧核对过。**改了解析/对齐行为，升 _V**。
         # v18（2026-09-18 §6.30）：**枚举块吸附**（align.peel_enum_blocks）。
         #     中译本把英文原版的**公式图内容**排成散文段（§1.7 的
         #     (IIIa)(IIIb)(IIIc) = 原版 eqn01_39a/b/c.jpg 三张图；§15.4 的
         #     (a)(b) 同理）→ 中文侧凭空多块 → DP 按「合并吸收孤儿块」处理
         #     → 整条链相位滑移、原位缺中文 → AI 补译 → 同一句中文出现两遍。
         #     修法：零 LLM 的四条判据把这类块从段落流摘出（不丢内容，改挂
         #     到对应公式组下面渲染）。**改了解析/对齐行为，必须升 _V**。
         # v17（2026-09-18 §6.29）：中文 md ①「断口」合并（PDF 换行把一个英文段
         #     拆成两段，凭空多出一块 → 段落流错位一格）② OCR 公式编号残渣行
         #     （1.398 / 1.3次）归 formula，不再当正文块参与 DP
         # v16：中文 md 的「缩进小标题」（前导空格的短行）提升为 heading
         #      —— 用户点名「序言~第二章 标题中文对齐错」的根因：英文侧吞掉标题、
         #      中文侧（raw md `### 前言`）有 → 该标题之下小节配对整体左偏一格。
         #      实测全书 5 处，全在卷头（Contents / Editor's foreword /
         #      PROBABILITY THEORY / THE LOGIC OF SCIENCE / Preface）。
         # v14：① CSS class 标题接受字母后缀（h2a/h2b）；② md 裸行附录子节
         #      标题（`A.1 …`）认成 h2 → 附录 A/B/C 的小节树重建
         # v3：块级保真（pre→code / 提示框 box / 列表 in_list），旧缓存无这些字段
         # v4：章首清理（页码块/重复章名丢弃、引语署名并入引语）
         # v5：正文段内的行内公式图（<img class="mi">）不再当插图搬出去
         # v6：装饰横线（`p.line_img` / 高≤4 宽≥300 的极扁图）不再进图位流
         #     —— 它会抢中文公式的配对、还把真公式编号顶到自己头上（§2.6）
         # v10：md 导入清洗（`<eq>…</eq>`→`$…$`；`![image](cdn…)` 整行剔除）
         #     —— 旧缓存里这两类原文会原样漏进成品（2026-09-17 用户截图）
         # v11：`_ZH_HEAD_RE` 补 `编者序`（§3.1）—— 它原本不算标题，导致中文前置的
         #     编者序被并进「出版信息」单元，EN Editor's foreword 只好配到「出版信息」
         #     （标题错 + 编者序内容被吞）。实测影响面：**只前置 2 组，尾部不动**。
         # v12：`_ZH_HEAD_RE` 补 `引用文献/参考文献/人名索引/术语索引/符号`，
         #     并新增 `_bare_head`（裸标题行识别）—— 中文版尾部这五节的标题
         #     要么是三级标题不在词表、要么压根是裸行，于是全被并进 md032/md033，
         #     导致 EN References(529) ↔ zh 附录C(1076)、Subject index ↔ 致谢。
         #     实测影响面：**只尾部 chapter30 以后**（预演见 dbg_frontmap --dry-fix）。
CACHE = Path(os.environ.get("PARSE_CACHE_DIR", ".cache/parse"))


def key_for(path, *extra) -> tuple | None:
    try:
        st = Path(path).stat()
    except OSError:
        return None
    return (str(Path(path).resolve()), st.st_mtime_ns, st.st_size, _V, *map(str, extra))


def _file(key) -> Path:
    # ⚠ 不能用内置 hash()：字符串 hash 按进程随机化（PYTHONHASHSEED），
    #   跨进程永远不命中。
    h = hashlib.sha256(repr(key).encode("utf-8")).hexdigest()[:20]
    return CACHE / f"{h}.json"


def load(key):
    if not key:
        return None
    f = _file(key)
    if not f.exists():
        return None
    try:
        return json.loads(f.read_text(encoding="utf-8"))
    except Exception:                                    # noqa: BLE001
        return None


def save(key, obj) -> None:
    if not key:
        return
    try:
        CACHE.mkdir(parents=True, exist_ok=True)
        f = _file(key)
        tmp = f.with_suffix(".tmp")
        tmp.write_text(json.dumps(obj, ensure_ascii=False), encoding="utf-8")
        tmp.replace(f)
    except Exception:                                    # noqa: BLE001
        pass
